/**
 * Shared Tailwind preset. Apps extend this via `presets: [preset]` and add their own `content`.
 * @type {import("tailwindcss").Config}
 */
const preset = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default preset;
